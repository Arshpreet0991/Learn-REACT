# Shopping List Example

- Create two components in react:
  - `ShoppingList`: which will contain our list of items and quantity. (PARENT COMPONENT)
    - This is a parent component because, the list resides here, all the changes made to list are here. The form component only handles the user inputs.
  - A form component `ShoppingForm`: this will handle the submit button from the user
